extends layout

block content

  h1 Item: #{item.name}
  p #{item.description}
  p Price: $#{item.price}
  p in Stock: #{item.stock}pcs

  hr
  p
    a(href=item.url+'/delete') Delete Item
  p
    a(href=item.url+'/update') Update Item